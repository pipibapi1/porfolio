extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div
      h1 My Portfolio
      p.lead.text-light
        | Hello! Welcome to my Portfolio. This is built using Nodejs + Expressjs + Pug template engine. The Pug views i'm using here completely belong to Joseph Butterfield. Anyone who is interested in the project can give credit to him via the original project github link: https://github.com/raleigh9123/Portfolio-Site-NodeJS-Express-and-Pug 
  article.grid-container.portfolio-index
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      each project in projects
        .cell
          a(href=`/projects/${project.id}`)
            if project.image_urls[0].landing
              img.thumbnail(src=project.image_urls[0].url alt=`Project-${project.id}`)
            h5=project.project_name